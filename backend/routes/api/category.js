const express = require("express");
const { getAllCategories } = require("../../controller/categoriesControllers");
const router = express.Router();

// router.get("/products/category", getAllCategories);

module.exports = router;